# Build settings live here rather than only in the Netlify dashboard, so they are
# reviewable and cannot drift without a commit.
#
# The build command is deliberately not set: this repo installs with pnpm, and
# Netlify already detects that from pnpm-lock.yaml.
[build]
  publish = "dist"
